# Archived Host Tools

This directory holds legacy or superseded host-tool implementations that were
previously staged outside the canonical `the-inner-loop/host-tools/` tree.

The active host tools are the non-archived files directly under
`the-inner-loop/host-tools/`.
