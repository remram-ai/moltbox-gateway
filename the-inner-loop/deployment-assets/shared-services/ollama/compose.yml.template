services:
  ollama:
    image: "${OLLAMA_IMAGE:-ollama/ollama:0.6.8}"
    container_name: "{{ container_name }}"
    restart: unless-stopped
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    volumes:
      - "{{ shared_root }}/data:/root/.ollama"
