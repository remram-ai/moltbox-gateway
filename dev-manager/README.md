# Dev Manager Archive

`dev-manager/` is no longer the canonical location for active inner-loop
documentation or host tooling.

Canonical paths:

- docs: `the-inner-loop/docs/`
- host tools: `the-inner-loop/host-tools/`
- active control-plane package: `the-inner-loop/control-plane/`

Any leftover material under `dev-manager/` should be treated as archived or
legacy reference material until it is either removed or explicitly migrated.
