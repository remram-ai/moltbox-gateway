# moltbox

`moltbox/` defines the MoltBox appliance. It is not the CLI software project.

Contents:

- `containers/` for container definitions and compose templates
- `hardware/` for appliance build and hardware reference material
- `config/` for deployment-time configuration templates

The active CLI implementation lives under `moltbox-cli/`.
