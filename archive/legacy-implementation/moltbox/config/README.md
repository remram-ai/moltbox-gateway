# MoltBox Config

This directory contains configuration templates used during deployment.

The baseline templates were copied from `archive/MoltBox-Legacy/` so the active tree starts from the last known good config set without modifying the archive copy.

Contents include:

- `.env` and container environment examples
- OpenClaw configuration templates under `openclaw/`
- service configuration templates such as `opensearch.yml`
