# Runtime Stack

The runtime domain operates OpenClaw environments.

Canonical form:

- `moltbox runtime <environment> <verb>`

Typical verbs:

- `deploy`
- `status`
- `logs`
- `start`
- `stop`
- `restart`

Runtime environments are deployment targets. Their compose assets live under `moltbox/containers/runtimes/`.
