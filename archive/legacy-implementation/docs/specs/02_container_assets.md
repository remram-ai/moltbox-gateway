# Container Assets

Appliance container assets live under `moltbox/containers/`.

Current groupings:

- `tools/` for the MoltBox tooling container definition
- `shared-services/` for appliance host services
- `runtimes/` for OpenClaw runtime assets

Host CLI targets correspond to the shared-service definitions in this tree.
