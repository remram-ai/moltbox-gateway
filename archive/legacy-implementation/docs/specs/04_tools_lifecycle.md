# Tools Lifecycle

The tools domain manages the MoltBox deploy tooling itself.

Canonical form:

- `moltbox tools <verb>`

Typical verbs:

- `version`
- `health`
- `status`
- `inspect`
- `update`
- `rollback`

This domain owns the Python package under `moltbox-cli/tools/src/`.
