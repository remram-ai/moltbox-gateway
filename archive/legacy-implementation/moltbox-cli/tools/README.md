# Tools Domain

`tools/` contains the singleton MoltBox tooling domain.

It owns:

- the shared Python package source under `tools/src/`
- tooling-domain diagnostics
- tooling-domain tests

Canonical grammar:

- `moltbox tools <verb>`
