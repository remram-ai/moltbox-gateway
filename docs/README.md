# Documentation

Repository documentation is centralized under `docs/`.

Primary references:

- `repository_taxonomy.md` for repository rules and ownership boundaries
- `cli/README.md` for the authoritative `moltbox` grammar
- `architecture/moltbox_cli_architecture.md` for CLI and domain architecture
- `specs/` for operational and deployment specifications
- `operator/README.md` for operator-facing entry points
- `operator/capability-integration.md` for the MoltBox implementation of the Remram capability integration standard
