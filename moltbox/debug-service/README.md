# Moltbox Debug Service

Host-level MCP debug service for the Moltbox runtime.

This package is installed and managed by `moltbox/scripts/70-debug-service.sh`.

Notable tooling includes runtime inspection, structured publish workflow control, and `chat_smoke_test` for one-off gateway chat/UAT probes against `test` or `prod`.
